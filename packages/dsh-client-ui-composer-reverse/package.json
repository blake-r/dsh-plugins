{
  "name": "@blake-r/dsh-client-ui-composer-reverse",
  "version": "0.1.0",
  "description": "DSH Web UI plugin: reverses the composer when it is pinned at the top — the stats dock moves above the input card and the dock row below it, and every composer popover (context meter, access-mode menu, stats dialogs, model menu) opens downward instead of upward so nothing gets clipped by the scroll container or hidden under the header.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "scripts": {
    "check": "node --check lib/index.js && node --check lib/client.js"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.2"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}