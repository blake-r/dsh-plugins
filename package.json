{
  "name": "dsh-plugins-repo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Monorepo of DeepSeek Harness (dsh) plugins, each published as its own npm package with dsh.bundle.patch for selective install.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "node scripts/check.mjs",
    "publish": "pnpm -r publish"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
